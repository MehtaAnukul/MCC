<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.anukul_pc.mcc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MCC"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="ExtraText">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignupActivity"
            android:label="Registration" />

        <activity
            android:name=".LoginActivity"
            android:label="Login" />
        <activity
            android:name=".Main4Activity"
            android:label="Deshboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Main5Activity"
            android:label="@string/title_activity_main5"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SelectVMActivity"
            android:label="SelectVM" />
        <activity
            android:name=".About_us"
            android:label="About_us" />
        <activity android:name=".DeshboardActivity" />
        <activity android:name=".CreateVMActivity" />
        <activity android:name=".SettingActivity"></activity>

    </application>

</manifest>